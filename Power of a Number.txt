Power of a Number
#include <stdio.h>
int main() {
    int n, P, result = 1;
    scanf("%d %d", &n, &P);

    for(int i = 1; i <= P; i++){
        result *= n;
}
    printf("%d", result);
    return 0;
}