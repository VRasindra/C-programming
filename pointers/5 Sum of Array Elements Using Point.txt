5: Sum of Array Elements Using Pointers ( )
#include <stdio.h>
int main() {
    int n, sum = 0;
    scanf("%d", &n);
    int arr[n];
    int *ptr = arr;
    for (int i = 0; i < n; i++) {
        scanf("%d", ptr + i);
    }
    for (int i = 0; i < n; i++) {
        sum += *(ptr + i);
    }
    printf("%d", sum);
    return 0;
}
