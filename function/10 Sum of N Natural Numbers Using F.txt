10 Sum of N Natural Numbers Using Function
#include <stdio.h>

int sumNatural(int n) {
    return n * (n + 1) / 2;
}

int main() {
    int N;
    scanf("%d", &N);
    printf("%d", sumNatural(N));
    return 0;
}
