Count Digits
#include <stdio.h>
int main() {
int n,num, count = 10;
scanf("%d", &num);
    while (num>0){
        num=num/10;
        count++;
    }
    printf("%d\n", count);
    return 0;
}